node_modules/
# Firebase service account keys (never commit)
*firebase-adminsdk*.json
*-firebase-adm*.json
photos/*.zip
# Local / accidental uploads (never commit)
photos/*.exe
photos/**/*.pdf
.firebase/
